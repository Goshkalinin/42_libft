String Length, очевидно.

	size_t strlen(const char *s);

## Нахватанное:
Функция strlen() вычисляет длину строки, на которую указывает s, исключая завершающий нулевой байт ('\0').

## Отсебятина:
Считает символы в строке до нуль-терминатора.